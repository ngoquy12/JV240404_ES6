const formatName = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName1 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName2 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName3 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName4 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName5 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};
const formatName6 = (fisrtName, lastName) => {
  return fisrtName + " " + lastName;
};

const PI = Math.PI;

const getAllUser = () => {
  return 1;
};

export {
  formatName,
  getAllUser,
  formatName1,
  formatName2,
  formatName3,
  formatName4,
  formatName5,
  formatName6,
};

export default PI;
